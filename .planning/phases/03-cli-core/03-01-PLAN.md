---
phase: 03-cli-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/package.json
  - cli/tsconfig.json
  - cli/src/ralph.ts
  - cli/src/commands/status.ts
  - cli/src/commands/list.ts
  - cli/src/lib/database.ts
  - cli/src/lib/output.ts
  - cli/src/lib/format.ts
autonomous: true

must_haves:
  truths:
    - "User can run `ralph status` and see all running loops with project, mode, runtime, cost"
    - "User can run `ralph list` and see all registered projects with path and status"
    - "CLI works without dashboard running (direct database access)"
    - "Output is colored and formatted as a readable table"
  artifacts:
    - path: "cli/package.json"
      provides: "CLI package configuration with commander, chalk, ora dependencies"
      contains: "\"bin\": { \"ralph\": \"./dist/ralph.js\" }"
    - path: "cli/src/ralph.ts"
      provides: "CLI entry point with Commander program"
      exports: ["program"]
    - path: "cli/src/commands/status.ts"
      provides: "Status command implementation"
      exports: ["statusCommand"]
    - path: "cli/src/commands/list.ts"
      provides: "List command implementation"
      exports: ["listCommand"]
  key_links:
    - from: "cli/src/ralph.ts"
      to: "cli/src/commands/status.ts"
      via: "Commander addCommand"
      pattern: "program\\.addCommand\\(statusCommand\\)"
    - from: "cli/src/commands/status.ts"
      to: "dashboard/server/processManager"
      via: "import getProcessRegistry"
      pattern: "from.*processManager"
    - from: "cli/src/commands/list.ts"
      to: "dashboard/server/database/repositories/ProjectRepository"
      via: "import getProjectRepository"
      pattern: "from.*ProjectRepository"
---

<objective>
Create the CLI scaffold with infrastructure and read-only commands (status, list)

Purpose: Establish CLI foundation that directly accesses the same data sources as dashboard, enabling terminal-based loop monitoring without requiring the dashboard to be running.

Output: Working `ralph status` and `ralph list` commands with formatted table output
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cli-core/03-RESEARCH.md
@.planning/phases/01-process-foundation/01-01-SUMMARY.md
@.planning/phases/01-process-foundation/01-02-SUMMARY.md

Key infrastructure already built:
- ProcessRegistry at dashboard/server/processManager/ProcessRegistry.ts
  - getProcessRegistry() returns singleton
  - getAllActiveLoops() returns ActiveLoopInfo[] with session data and liveness
- ProjectRepository at dashboard/server/database/repositories/ProjectRepository.ts
  - getProjectRepository() returns singleton
  - listProjects() returns LauncherProject[]
  - getProject(id), getProjectByPath(path) for resolution
- SessionRepository has getActiveSessions() for running loop data
- Database at ~/.ralph/ralph.db, PID files at ~/.ralph/pids/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLI package structure and dependencies</name>
  <files>
    cli/package.json
    cli/tsconfig.json
  </files>
  <action>
    Create cli/ directory at project root (sibling to dashboard/).

    Create cli/package.json:
    - name: "@ralph/cli"
    - type: "module" (ESM)
    - bin: { "ralph": "./dist/ralph.js" }
    - dependencies: commander@14.x, chalk@5.x, ora@8.x, better-sqlite3@12.x (must match dashboard version)
    - devDependencies: @types/node, @types/better-sqlite3, typescript
    - scripts: build, dev, link (for npm link)

    Create cli/tsconfig.json:
    - target: ES2022
    - module: NodeNext
    - moduleResolution: NodeNext
    - outDir: ./dist
    - rootDir: ./src
    - strict: true
    - Include path mapping to import from dashboard/server/** via relative paths

    The CLI will import dashboard modules directly via relative paths (e.g., ../dashboard/server/processManager). This avoids npm workspace complexity while sharing code.

    Run: cd cli && npm install
  </action>
  <verify>
    - cli/package.json exists with correct bin field
    - cli/tsconfig.json exists with ESM config
    - cd cli && npm install succeeds
    - node_modules contains commander, chalk, ora
  </verify>
  <done>CLI package initialized with all dependencies installed</done>
</task>

<task type="auto">
  <name>Task 2: Create CLI entry point and output utilities</name>
  <files>
    cli/src/ralph.ts
    cli/src/lib/database.ts
    cli/src/lib/output.ts
    cli/src/lib/format.ts
  </files>
  <action>
    Create cli/src/ralph.ts:
    - Add shebang: #!/usr/bin/env node
    - Import Commander and create program
    - Set name('ralph'), description, version('3.0.0')
    - Import and add status, list commands (stub imports for now)
    - Call program.parseAsync(process.argv)

    Create cli/src/lib/database.ts:
    - Re-export database initialization from dashboard
    - Import path and os to construct ~/.ralph/ralph.db path
    - Export initializeDatabase() that ensures DB is ready
    - Export getters for repositories (re-export from dashboard modules)
    - Use relative import: import { getDb } from '../../dashboard/server/database/index.js'

    Create cli/src/lib/output.ts:
    - Import chalk and ora
    - Export styled output functions:
      - success(msg), error(msg), warn(msg), info(msg)
      - spinner(msg) returns ora instance
      - table header/row formatting helpers
    - Export color constants: colors.success (green), colors.error (red), etc.

    Create cli/src/lib/format.ts:
    - Export formatDuration(startedAt: string): string - returns "2h 15m" format
    - Export formatCost(cost: number): string - returns "$1.23" format
    - Export formatStatus(isAlive: boolean): string - returns colored "running" or "dead"
    - Export padColumn(text: string, width: number): string - for table alignment
  </action>
  <verify>
    - cd cli && npx tsc --noEmit passes (type checking)
    - cli/src/ralph.ts has shebang and Commander setup
    - cli/src/lib/*.ts files exist with exports
  </verify>
  <done>CLI entry point and utility modules created with type-safe exports</done>
</task>

<task type="auto">
  <name>Task 3: Implement status and list commands</name>
  <files>
    cli/src/commands/status.ts
    cli/src/commands/list.ts
    cli/src/ralph.ts
  </files>
  <action>
    Create cli/src/commands/status.ts:
    - Export statusCommand using Commander
    - Description: "Show all running loops system-wide"
    - Action (async):
      1. Import and call getProcessRegistry()
      2. Call registry.getAllActiveLoops()
      3. If empty, print "No running loops" and exit
      4. Print table with columns: PROJECT (20), MODE (12), RUNTIME (12), COST (10), STATUS
      5. Use chalk for headers (dim) and status colors (green=running, red=dead)
      6. For each loop: show projectId, mode, formatDuration(startedAt), formatCost(costSpent), formatStatus(isAlive)

    Create cli/src/commands/list.ts:
    - Export listCommand using Commander
    - Description: "Show all registered projects"
    - Action (async):
      1. Import and call getProjectRepository()
      2. Call repo.listProjects()
      3. If empty, print "No projects registered" with hint to add via dashboard
      4. Also get active sessions to show status per project
      5. Print table with columns: NAME (25), PATH (40), STATUS (10)
      6. Status: "running" (green) if active session exists, "-" otherwise

    Update cli/src/ralph.ts:
    - Import statusCommand and listCommand
    - Add both commands to program

    Build and link:
    - cd cli && npm run build
    - cd cli && npm link
  </action>
  <verify>
    - ralph status runs without errors (shows "No running loops" or table)
    - ralph list runs without errors (shows projects or "No projects registered")
    - ralph --help shows both commands with descriptions
    - Output is colored (green/red status, dim headers)
  </verify>
  <done>`ralph status` shows running loops, `ralph list` shows registered projects</done>
</task>

</tasks>

<verification>
Overall phase checks:
1. CLI binary works: `ralph --version` returns "3.0.0"
2. Status command: `ralph status` runs and shows formatted output
3. List command: `ralph list` runs and shows formatted output
4. No dashboard dependency: Commands work with dashboard stopped
5. Database access: CLI reads from ~/.ralph/ralph.db
6. Type safety: `cd cli && npx tsc --noEmit` passes
</verification>

<success_criteria>
- [x] CLI scaffold created with Commander.js
- [x] `ralph status` displays running loops with project, mode, runtime, cost, status
- [x] `ralph list` displays registered projects with path and running status
- [x] CLI works independently of dashboard (direct database access)
- [x] Output is formatted as readable table with colors
- [x] npm link makes `ralph` command globally available
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-core/03-01-SUMMARY.md`
</output>
